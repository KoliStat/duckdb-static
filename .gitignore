# CI build inputs/outputs — never committed (produced by the workflow).
duckdb/
build/
duckdb-static-*/
*.zip

# Local artifact-verification scratch (downloaded release zip + extracted libs).
_verify/
